(function () {var        form = $('.form'),        cache_width = form.width(),        a4 = [595.28, 841.89]; // for a4 size paper width and height        $('#create_pdf').on('click', function () {$('body').scrollTop(0);        createPDF();        });//create pdf        function createPDF() {        getCanvas().then(function (canvas) {        var                img = canvas.toDataURL("image/png")                ,                doc = new jsPDF({                unit:'px',                        format:'a4'                }                );<?php$this->load->library('encryption');$data_id=$this->uri->segment(3);$data_id = str_replace( "~", "/",  $data_id );$data_id = str_replace( "-", "=",  $data_id );$data_id = str_replace( ".", "+",  $data_id );$book_num= $this->encryption->decode($data_id);?>		var val=$book_num;		doc.addImage(img, 'JPEG', 20, 20);doc.save(val+'invoice.pdf');form.width(cache_width);});}// create canvas objectfunction getCanvas(){    form.width((a4[0] * 1.33333) - 80).css('max-width', 'none');    return html2canvas(form, {    imageTimeout: 2000,            removeContainer:true});	}}());